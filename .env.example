# ============================================
# 环境变量模板 —— 复制本文件为 .env 后填入真实 Key
#   Windows:  copy .env.example .env
#   Linux:    cp .env.example .env
# ⚠️ .env 含密钥，已被 .gitignore 排除，切勿提交
# ============================================

# 通义千问大模型（阿里云百炼）
# 获取: https://bailian.console.aliyun.com/ → 右上角 → API-KEY
MODEL=qwen-plus
# 可选项: qwen-plus(推荐) / qwen-turbo(最快最省但工具调用偏弱) / qwen3-max(最强)
# 不确定哪个可用，运行: python check_models.py
DASHSCOPE_API_KEY=sk-你的通义千问Key

# 高德开放平台（地图 API + 天气 API）
# 获取: https://console.amap.com/ → 应用管理 → 创建应用 → 添加 Key
# ⚠️ 服务平台必须选「Web 服务」；需完成个人认证
# 地图免费额度: 地理编码/路径规划/POI 各 5000 次/天；天气 30 万次/天
AMAP_API_KEY=你的高德Web服务Key
